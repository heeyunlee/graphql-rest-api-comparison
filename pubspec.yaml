name: graphql_rest_comparison
description: A new Flutter project.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.2 <3.0.0"
  flutter: ">=3.7.3"

dependencies:
  cupertino_icons: ^1.0.5
  flutter:
    sdk: flutter
  graphql: ^5.1.3
  http: ^0.13.5
  path_provider: ^2.0.12

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  fonts:
    - family: Rubik
      fonts:
        - asset: assets/fonts/Rubik-Light.ttf
          weight: 300
